<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.or.ddit.board.club.dao.ClubBoardDAO">

	<!-- 사내 동호회 목록 -->
		<select id="selectClubList" parameterType="string" resultType="ClubVO">
			SELECT *
			FROM CLUB
			WHERE EMP_ID = #{empId,jdbcType=VARCHAR} AND ISUSE = 0
		</select>
		
	<!-- 사내 동호회 등록 -->
	
	<!-- 사내 동호회 수정 -->
	
	<!-- 사내 동호회 삭제 -->
	
	<!-- 사내 동호회 신청(조회) -->
	
	<!-- 사내 동호회 찜 -->
	
	<!-- 사내 동호회 검색 -->

</mapper>